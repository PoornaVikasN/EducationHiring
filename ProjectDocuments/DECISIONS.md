# DECISIONS.md — EduHire

> Single source of truth for **what we decided and why** while spinning EduHire out of
> RxJobs4U. Read this before the build. Anything marked **OPEN** must be confirmed with the
> client before it is wired for money.

EduHire is a **Teacher Hiring Platform** — a sister project to RxJobs4U (a healthcare job
marketplace already built and shipped). We reuse the entire RxJobs4U architecture and swap the
domain from healthcare to education. Schools/recruiters hire teachers.

---

## 1. Locked decisions

| # | Decision | Rationale |
|---|---|---|
| D1 | **Brand: EduHire.** Logo splits as `Edu` (accent) + `Hire` (white), mirroring RxJobs4U's `Rx` + `Jobs4U`. | Client choice. |
| D2 | **Prototype-first** for the first client demo: landing + all 3 role logins + basic role dashboards, clickable. Full foundation docs (this folder) written first, then the build. | Demo deadline; auth flows must be real, not mockups. |
| D3 | **Drop SOS entirely.** Full-time job posts only. Remove the SOS job type, SOS subscriptions (both sides), SOS pricing, SOS landing section, seeker SOS access. | Client: "no SOS". Substitute-teacher mode can be added later if requested. |
| D4 | **New repo, separate folder.** `Desktop/EduHire/` (`EduHire-Frontend`, `EduHire-Backend`, `ProjectDocuments`). RxJobs4U is **never modified** — it is the reusable base we copy from. | Keep the base pristine for future sister apps. |
| D5 | **Copy whole apps, then rebrand the copies.** Not a selective file-by-file copy. | Login needs the auth module + API client + JWT/cookie plumbing together; wholesale copy is faster and reliable. |
| D6 | **No CRM.** The client flowchart shows CRM integration — we omit it. | Out of scope. |
| D7 | **Chatbot deferred.** When added, it is a **third-party widget embed (a link/script)**, not custom-built. | Client direction. |
| D8 | **Chat is basic, with file upload.** Real-time text chat (500-char limit) + file attachments, unlocking at SHORTLISTED. Not a WhatsApp-grade client (no threads, reactions, presence beyond online/typing-optional). | Client: "basic one enough… give file upload as well." |
| D9 | **Keep RxJobs4U's full money/notification/realtime infrastructure** (Razorpay + HMAC webhook, SystemConfig, Socket.IO, web-push VAPID, Brevo email, MSG91 SMS, S3 uploads). No Redis/BullMQ. No Firebase. No nodemailer. | Proven, domain-agnostic; reuse as-is. |
| D10 | **Per-job admin approval** (NEW — not in RxJobs4U). A posted job enters `PENDING_APPROVAL`; admin **Approves** (→ live) or **Rejects** (→ recruiter edits & resubmits). | Required explicitly by the client flowchart ("Job Goes for Admin Approval → Approved / Rejected → Modify & Resubmit"). |
| D11 | **Demo depth = "Stronger":** landing + 3 logins + 3 dashboards **plus** the key role screens relabeled (teacher profile/browse jobs, recruiter post-job/applicants, admin users/jobs tables). | Screens already exist from the copy; low marginal effort, far more convincing demo. |
| D12 | **Premium landing page** — a deliberate ~3x upgrade over RxJobs4U's landing (see §6), reusing RxJobs4U's header/hero **CSS** (transparent→solid header, full-bleed 100vh banner, scroll-reveal) but with richer education-specific sections and imagery. | Client wants the landing to be the standout of the demo. |

---

## 2. Pricing & payments — FINAL (fully admin-controlled, both sides)

Every amount AND every "is this side paid or free?" toggle lives in **SystemConfig** and is
editable from the **admin panel** (the same mechanism RxJobs4U uses to manage prices). Nothing
payment-related is hardcoded or permanently deleted — the admin decides what is active.

**Current defaults:**
- **School / recruiter — paid:** **₹500 / month = unlimited** job posts (Razorpay subscription).
  **Free tier = 2 active posts / month** with no subscription.
- **Teacher / job seeker — free:** teacher-side payment toggle is **off** by default.

**Admin can, from the panel:** change any amount, change the free-tier limit, and flip **either
side's** paid↔free toggle at any time.

| SystemConfig key | Type | Default | Meaning |
|---|---|---|---|
| `RECRUITER_MONTHLY_PAISE` | price | `50_000` | ₹500/mo — unlimited posts (school subscription) |
| `FREE_TIER_JOB_LIMIT` | setting | `2` | free active posts/month when school not subscribed |
| `SCHOOL_PAID_ENABLED` | setting (bool) | `true` | is school posting paid at all? |
| `TEACHER_PAID_ENABLED` | setting (bool) | `false` | does the teacher pay a shortlist fee? (off = free) |
| `APPLICATION_FEE_PAISE` | price | `9_900` | teacher shortlist fee — **retained, unused while toggle off** |

**Application flow is toggle-driven** (machinery kept from RxJobs4U, not deleted):
- Teacher-pay **off** (today): `INTERESTED → SHORTLISTED → WON | CLOSED`. No fee, no pay window.
  School identity revealed to the teacher at **SHORTLISTED**; chat also unlocks at SHORTLISTED.
- Teacher-pay **on** (if admin enables): RxJobs4U's path returns —
  `INTERESTED → SHORTLISTED → PAID → WON | CLOSED` with pay-window cron + reveal-on-PAID. Code
  stays in place, gated behind `TEACHER_PAID_ENABLED`.

**Removed (SOS-specific only):** `SOS_MONTHLY_PAISE`, `SOS_SEEKER_MONTHLY_PAISE`, `BOOST_PAISE`.

## 2b. Open items — confirm with client (do NOT block the demo)

| # | Question | Working assumption |
|---|---|---|
| O1 | **Subject list.** | Proposed: PRIMARY, ENGLISH, MATHEMATICS, SCIENCE, PHYSICS, CHEMISTRY, BIOLOGY, SOCIAL_STUDIES, HINDI, COMPUTER_SCIENCE. |
| O2 | **International Teacher Training** (client marks "Optional"). | Static informational page for now; "express interest" form later. |

---

## 3. Domain substitution map

| RxJobs4U (healthcare) | EduHire (education) |
|---|---|
| Hospital | School |
| Job Seeker (doctor/nurse) | Teacher |
| Recruiter (hospital admin) | School / Recruiter (Principal / HR) |
| HospitalDepartment (LAB, ICU, …) | Subject (Maths, Science, English, Primary, …) |
| Expertise (medical specialties) | Subjects taught / specializations |
| Degrees (MBBS, BDS, …) | B.Ed, M.Ed, B.A/M.A, B.Sc/M.Sc, B.Com, Ph.D, D.El.Ed |
| SOS (medical emergency) | **REMOVED** |
| Medical council reg. / indemnity / surgical cover | **REMOVED** |
| `HOSPITAL_PHOTO` upload kind | `SCHOOL_PHOTO` |

### New, education-specific (from client spec + flowchart)
- **`TeacherPost`** (a.k.a. "Post Applied For"). **Expanded 2026-07-09** (Phase 2) from the original 4-value client list to match every role already live in the job-posting UI: `SGT`, `TGT`, `PGT`, `PRE_PRIMARY_TEACHER`, `HM`, `PRINCIPAL`, `VICE_PRINCIPAL`, `SPECIAL_EDUCATOR`, `LAB_ASSISTANT`, `LIBRARIAN`, `COUNSELOR`, `IIT_JEE_FACULTY`, `NEET_FACULTY`, `OTHER`. Client confirmed: expand to match the shipped product rather than trim the UI down to the original 4.
- **`WorkLocationPreference`** (a.k.a. "Preferred Work Location"): `LOCAL`, `NATIONAL`, `INTERNATIONAL`.
- **`Subject`** — **expanded 2026-07-09** (Phase 2) from the original 10-value list to match every subject already live in the job-posting/teacher-profile UI: `PRIMARY`, `ENGLISH`, `MATHEMATICS`, `SCIENCE`, `PHYSICS`, `CHEMISTRY`, `BIOLOGY`, `SOCIAL_STUDIES`, `HINDI`, `COMPUTER_SCIENCE`, `TELUGU`, `HISTORY`, `GEOGRAPHY`, `ECONOMICS`, `BUSINESS_STUDIES`, `ACCOUNTANCY`, `INFORMATION_TECHNOLOGY`, `ART_CRAFTS`, `MUSIC`, `DANCE`, `ENVIRONMENTAL_SCIENCE`, `PRE_PRIMARY_EDUCATION`, `SPECIAL_EDUCATION`, `SCHOOL_ADMINISTRATION`, `COUNSELING`, `OTHERS`.
- **`JobDepartment`** (new 2026-07-09, Phase 2) — grade-level/section for a job posting, distinct from `Subject` (what's taught): `PRE_PRIMARY`, `PRIMARY`, `SECONDARY`, `SENIOR_SECONDARY`, `ARTS_CRAFTS`, `COMPUTER_SCIENCE`, `PHYSICAL_EDUCATION`, `ADMINISTRATION`, `LIBRARY`, `COUNSELING`, `OTHER`. Replaces a previously un-enumerated free-text field that was duplicated with drift across the job-post and job-edit frontend pages.
- **`UploadKind`** additions: `INTRO_VIDEO` (teacher introduction video), `CHAT_ATTACHMENT`.
- **`JobType`**: **removed entirely** (2026-07-08, see §11 D42-superseding) — there is no job-type concept at all, only one kind of posting.
- **`NotificationKind`**: rename `HOSPITAL_*` → `SCHOOL_*`; drop SOS-related kinds.

---

## 4. The demo shortcut (be honest about this)

For the **first clickable demo**, we do not do the full backend code-level rename in one pass.
Instead:
- Backend runs essentially as the RxJobs4U copy (auth/users/roles are domain-agnostic and work
  immediately) pointed at a fresh `eduhire` Mongo DB.
- Role enum **values** stay `JOB_SEEKER` / `RECRUITER` / `ADMIN` in the DB but **display** as
  "Teacher" / "School" / "Admin" in the UI.
- We rebrand the visible surface: theme, logo, landing copy, nav labels, and the most
  demo-visible domain strings ("Hospital profile" → "School profile", department → subject).

The **proper code-level rename** (`Hospital→School`, `JobSeeker→Teacher`, new enums, remove
medical fields) is **Phase 2**, done after the demo. Target docs in this folder describe the
clean end-state; this section is the only place that records the demo compromise.

---

## 5. Phase roadmap

- **Phase 0 — Demo (today, "Stronger" depth):** npm install + clean `.next` + point `.env` at
  fresh `eduhire` DB + seed admin → **premium landing rebuild** (§6) + theme/logo rebrand →
  relabel key role screens (teacher profile/browse jobs, recruiter post-job/applicants, admin
  tables) → auth golden path works for all 3 roles → run both apps.
- **Phase 1 — Foundation docs:** this folder (done before the build per house rule).
- **Phase 2 — Backend domain rename + admin job-approval:** Hospital→School, JobSeeker→Teacher,
  new enums, remove medical-specific fields, relabel NotificationKind, add `PENDING_APPROVAL`
  job status + admin approve/reject + recruiter resubmit (D10). Keep infra generic.
- **Phase 3 — Chat module:** ChatModule + Message schema + ChatGateway (`/chat` namespace,
  reuse the JWT-handshake pattern from notifications gateway) + file upload via existing S3
  uploads. Unlocks at SHORTLISTED. FE chat panel both sides.
- **Phase 4 — Domain features:** intro-video upload, personalized recommendations, advanced
  search/filters (location/subject/salary/experience), International Teacher Training page,
  chatbot widget embed, pricing finalization.

---

## 6. Landing page design (demo centerpiece — ~3x better than RxJobs4U)

**Goal:** the landing page is the standout of the demo. Reuse RxJobs4U's proven header/hero
**CSS mechanics**, but make it richer, more polished, and education-specific.

**Inspiration** (researched): Teach Away, Schrole, Eteach, TES, Teach Starter. Common winning
patterns we adopt: one bold outcome-focused headline over a full-bleed emotive classroom photo;
one primary + one low-commitment secondary CTA; a trust line; oversized expressive type;
"apply in one click" simplicity messaging; warm, human imagery.

**Carry over from RxJobs4U (CSS/mechanics — keep as-is, just retheme):**
- Transparent-to-solid sticky header: transparent over the hero, transitions to solid
  (blurred, dark) after ~80px scroll. Logo always visible ("Edu" accent + "Hire" white).
- Full-bleed **100vh hero** with a background banner image + dark bottom-weighted gradient
  overlay; content (badge → H1 slogan → subtext → CTAs → trust pills) anchored low.
- Scroll-reveal system (`LandingReveal` IntersectionObserver + `.will-reveal*` classes);
  hero keyframe entrance; `ScrollToTop`; `.btn-glow`.
- The 4-property gradient-text rule (background + WebkitBackgroundClip + WebkitTextFillColor +
  backgroundClip + color:transparent) — keep, it's a known bug-trap.

**Theme:** professional education palette — indigo/blue primary (e.g. `#3949ab`) with a warm
accent (amber/coral) for CTAs. One-file swap in `app/globals.css` `@theme` block.

**Hero copy (draft — refine during build):**
- H1 slogan: e.g. *"Find the teaching role you were meant for."* / for schools: *"Hire great
  teachers, faster."* (audience-aware or a single unifying line).
- Subtext: one sentence on what EduHire is + who it's for.
- Primary CTA "Find Teaching Jobs", secondary "Hire Teachers".
- Trust pills: "Verified Schools", "Local · National · International", "Free for Teachers".

**Section stack (below hero), each scroll-revealed:**
1. **Stat / trust band** — honest, generic (no fake numbers): "Free for teachers", "Posts
   reviewed by admin", "Chat directly with schools".
2. **For Teachers** — 3–4 steps: build profile (resume + intro video) → browse & filter →
   apply in one click → chat with schools.
3. **For Schools** — 3–4 steps: post a job (2 free/month) → admin-approved & live → search &
   shortlist teachers → chat directly. Mention ₹500/mo unlimited.
4. **Subjects / posts strip** — visual chips: SGT · PGT · HM · Principal; Maths · Science ·
   English · Primary…
5. **Why EduHire** — compact 3-column feature card grid (real-time chat, verified schools,
   intro-video profiles, international opportunities).
6. **International Teacher Training** teaser (optional feature) — small section/banner.
7. **Pricing** — 2 cards: Teachers (Free) · Schools (Free 2/mo or ₹500/mo unlimited).
8. **Testimonials** — generic placeholder cards.
9. **Final CTA band** + footer.

**Implementation:** rewrite `app/page.tsx`; retheme `app/globals.css`; rebrand
`common-components/site-header.tsx`. Use education imagery from Pexels/Unsplash (free,
hotlinkable). Reuse the landing-page design blueprint patterns already proven in RxJobs4U.

## 7. Reference
- Client flowchart: `RxJobs4U/rxjobs4u-frontend/app/recruiter/dashboard/WhatsApp Image 2026-05-21 at 11.29.12 PM.jpeg`
- Reference dummy site (thin app shell, 3 roles): `https://vebspot.com/school/school-teacher-app.html`
- Landing inspiration: teachaway.com, schrole.com, eteach.com, tes.com, teachstarter.com
- Base codebase: `Desktop/RxJobs4U/` (do not modify).

---

## 8. Rebrand — 2026-07-05

| # | Decision | Rationale |
|---|---|---|
| D13 | **Public brand name is "School Teacher"** (with a space). Folder + npm package names stay `EduHire` / `eduhire-*`. Every user-facing string (docs, page titles, `<title>`, email copy, notification body, landing hero) uses "School Teacher". Internal identifiers stay lowercase-hyphen so nothing breaks. | Client renamed; landing page already updated. Path/package rename is unnecessary risk. |
| D14 | **Substitute-teacher mode is back** as `PostingType.URGENT`. D3 is superseded — the platform now supports both `URGENT` (short-notice sub) and `PERMANENT` (long-term). School and teacher can subscribe to urgent monthly access. | Later client request; matches the healthcare SOS pattern that already works. |

---

## 9. Hardened decisions inherited from RxJobs4U Phase 3a–3f — 2026-07-05

These are the non-obvious calls we made in the sibling project that we're pre-committing to here from day 1. Every one is either a bug-pattern we lived through or an efficiency lesson.

### Security & infra

| # | Decision | Why |
|---|---|---|
| D15 | **Custom body-only Mongo-operator sanitizer.** Do NOT use `express-mongo-sanitize` — it mutates `req.query` in place, which crashes on Node 20+ (read-only getter). Custom recursive walker on `req.body` only. | Lost an evening to this in RxJobs4U. Query/params aren't the attack vector; DTOs type-coerce them. See BUG_PATTERNS §Phase 3e. |
| D16 | **Helmet COOP = `same-origin-allow-popups`** (not the default `same-origin`). | Default blocks Google OAuth popup `postMessage`-back — sign-in silently hangs. Same fix likely clears reCAPTCHA TypeErrors from the badge iframe. |
| D17 | **`safeEqual` utility for all HMAC compares** (Razorpay, future Meta webhooks). Never `===` / `!==` on signatures. | Timing-attack; caught in RxJobs4U's `eacdadd`. Single helper in `common/utils/crypto.util.ts`. |
| D18 | **`verifyUploadKey` is opt-in per persist site** (not a global interceptor). Wire into `UsersService.update*`, `SchoolsService.create/update` on any field bearing an S3 URL. | RxJobs4U shipped presign without this and had trust-the-client bugs. HeadObject at persist time is cheap and closes the class. |
| D19 | **Body size cap explicit at 1 MB** for `json` + `urlencoded`. File uploads bypass the BE entirely (S3 presign). | Cheap DoS defence + locks behaviour vs framework default drift. |
| D20 | **Env validation via Joi at boot** — fail-fast. `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `CONFIG_ENCRYPTION_KEY` all ≥32 chars and **mutually distinct**. Integration keys required only in `NODE_ENV=production`. | Prod misconfig is expensive; boot-time death is cheap. |
| D21 | **`tokenVersion` claim** in JWT, revalidated on every request. Bumped on password change / reset / forced logout via `$inc: {tokenVersion:1}`. | Otherwise old access tokens live for 15 min after a credential change. |
| D22 | **`PUBLIC_FRONTEND_URL` env for every outbound user-facing link.** Never hardcode the FE domain in BE. | RxJobs4U had a `.in` ghost hardcoded in 5 places, found only via repo-wide grep. |
| D23 | **PII redaction at log-emission** via `redactEmail` / `redactPhone`, PLUS pino redact config for Authorization/cookie/password fields. | Belt + suspenders — one catches app-logs, other catches structured objects. |
| D24 | **Anonymous auth audit** — `AuditService.logAuthEvent(action, masked, reason, ip, ua)` for `AUTH_FAILED`, `OTP_FAILED`, `OTP_LOCKED`, `PASSWORD_RESET`, `LOGIN_SUCCESS`. Nullable `adminId` on `AuditLog`. | Forensics for credential-stuffing detection; must be forensics-ready before public launch. |

### Data model

| # | Decision | Why |
|---|---|---|
| D25 | **All geo fields use a `@Schema({_id:false})` LocationSchema sub-class** with `default: null` on the parent `@Prop`. NEVER inline `{type: {type:String,enum:['Point']}, coordinates:[Number]}`. | Mongoose 9 crashes at schema-build on inline with `default: null` or `default: undefined`. Sub-schema class accepts `null` cleanly. |
| D26 | **`type: String` explicit** on every `string \| null` `@Prop` even when TS type is unambiguous. | Without it NestJS Mongoose throws `CannotDetermineTypeError` at boot for union types. |
| D27 | **Never mix `@Prop({index:true})` and `Schema.index()`** on the same field. `@Prop({index:true})` for single-field; `Schema.index()` for compound. | Duplicate index warnings + Atlas may refuse to build. |
| D28 | **`{returnDocument:'after'}` on `findOneAndUpdate`, never `{new:true}`.** | Mongoose 9 dropped the legacy option; docs may be stale silently. |

### Pricing & config

| # | Decision | Why |
|---|---|---|
| D29 | **Pricing is 100% dynamic via `SystemConfig{type:'price'}`.** No hardcoded rupee constants. `getPricePaise(key)` throws if the key isn't configured. Admin fills at go-live via UI. | Departs from RxJobs4U (which had ₹399/₹299/₹99 in a `pricing.ts` file). Client wants pricing flexibility without deploys. |
| D30 | **SystemConfig settings carry `displayKind` / `unit` / `maxValue` metadata.** FE admin panel branches on `displayKind === 'boolean'` → `<Switch>` render, else `<Input type="number">` + unit suffix. | Otherwise the admin panel shows "Currently set to 1 km" for a 0/1 toggle. |
| D31 | **Boot-time metadata back-fill** for pre-existing SystemConfig docs. Idempotent — only writes changed fields. | Lets us evolve setting metadata without hand-migrating in prod. |

### Forms & UI

| # | Decision | Why |
|---|---|---|
| D32 | **Discriminated forms = discriminated `useForm` instances.** For `type: URGENT \| PERMANENT` posting form, render two separate child components with their own `useForm<VariantValues>()` + variant-specific Zod schema. NEVER one form with a union type. | Otherwise `errors.<field>` becomes uncastable without `as Partial<...>` everywhere. |
| D33 | **Type-aware display helpers** live in `lib/utils/postings-display.ts`. Every card / detail / dialog that renders a Posting branches on `type === URGENT` to use `urgentQualificationLabel`, `urgentFeeDisplay`, `formatShiftAt`, `formatShiftRange` instead of the salary/experience formatters. | Auto-derived legacy columns don't display meaningfully for urgent — showing "0-0 yrs experience" or "₹240 LPA" on a shift is worse than showing nothing. |
| D34 | **Notifications bell fetches on mount** (not only on open). | Otherwise the badge count is wrong on first render until user clicks. |

### Ops

| # | Decision | Why |
|---|---|---|
| D35 | **`COOKIE_DOMAIN` is blank in local dev.** Set only in prod (`.schoolteacher.com`). | Browser rejects `Domain=.schoolteacher.com` on `localhost` → refresh cookie silently dropped → looks like login is broken. |
| D36 | **Next.js 16 uses `proxy.ts`** (not `middleware.ts`). Public paths allow-list is the single source of truth for what's not gated. Any new public route → add to `PUBLIC_PATHS` **same commit**. | RxJobs4U had `/privacy-policy` and `/terms` bounce to `/login` for a week because they weren't in the list. |
| D37 | **Turbopack lazy-compiles per route.** Transient 404s after edits are normal. Fix: hard-refresh browser (`Ctrl+Shift+R`). Persistent: `rm -rf .next && npm run dev`. Not a bug. | Documented so future contributors don't chase ghosts. |

### Chat (Phase 1 planned)

| # | Decision | Why |
|---|---|---|
| D38 | **Chat rooms scoped 1:1 with applications.** `roomId === applicationId`. Access checked live on join/send against the application's current state (no separate room-provisioning step). Both parties (teacher + school-admin) joined server-side; anyone else → `disconnect()`. | Simplest scoping; no leaks; matches user's D8 (basic chat with file upload). **Corrected 2026-07-08 — see D44**: this was implemented as PAID/WON-only, which is wrong; see D44 for the fix. |
| D39 | **Chat file uploads** reuse the presign flow with `UploadKind.DOCUMENT` (up to 5 MB, PDF / JPG / PNG). | Reuses infra; no new infra required. **Status 2026-07-08: not yet implemented** — chat is currently text-only. Tracked as Phase 4 follow-up work. |
| D40 | **Message body max 4000 chars**, validated FE + BE. Longer than the D8 "500-char" spec because file attachments carry most of the weight; long text is fine. | Reconciling D8's 500-char intent with practical UX; if client insists on 500, revisit. **OPEN**. **Status 2026-07-08**: current implementation is actually 2000 chars — split the difference by accident, not deliberately. Still open for a final call. |

---

## 11. Production cleanup pass — 2026-07-08

Demo phase is over — EduHire is going to production. This session did the deferred "demo shortcut" cleanup from §4 and fixed a live chat bug found while doing it.

| # | Decision | Rationale |
|---|---|---|
| D41 | **Full backend domain rename executed**: `Hospital`→`School` (module, schema, controller, service, DTOs, the `hospitalId` field on every schema that had it), `Role.JOB_SEEKER`→`Role.TEACHER` (enum member **and** string value), `HospitalDepartment`→`Subject` (PRIMARY/ENGLISH/MATHEMATICS/SCIENCE/PHYSICS/CHEMISTRY/BIOLOGY/SOCIAL_STUDIES/HINDI/COMPUTER_SCIENCE) + `TeacherPost` (SGT/PGT/HM/PRINCIPAL, per §3), `HospitalSize`→`SchoolSize`, `NotificationKind.HOSPITAL_*`→`SCHOOL_*`, `UploadKind` gained `SCHOOL_PHOTO` (was referenced on the FE but never actually existed on the BE — a real gap, now fixed). Frontend mirrored the same rename (`lib/api/hospitals.ts`→`schools.ts`, `/admin/hospitals`→`/admin/schools`, etc). Done now, pre-production, because the only data on the fresh `eduhire` Mongo DB is seeded demo/admin accounts — this is the cheapest point to do a real rename rather than a migration. | §4's "demo shortcut" explicitly deferred this to "Phase 2, after the demo" — the demo is over. |
| D42-superseding | **No job types at all — supersedes D14 (urgent/substitute mode via `PostingType.URGENT`), reverting to D3's original "no SOS" intent.** `JobType` enum deleted entirely (front + back) — there is only ever one kind of job posting. `PaymentKind.SEEKER_SOS_SUBSCRIPTION`/`JOB_POST` deleted, the `sos-subscription` stub page and "SOS Job Alerts" UI text deleted. | Client confirmed 2026-07-08: "We don't have any urgent jobs, only FT jobs simply — no job types needed at all." |
| D43 | **Job-posting gating rewritten to actually match §2's pricing model.** `jobs.service.ts` previously still ran RxJobs4U's original per-post ₹299 payment flow (`JobStatus.PENDING_PAYMENT`) — it never implemented §2's subscription-or-free-tier model at all, despite §2 having been decided earlier. Now: a new post goes `ACTIVE` immediately if the school has an active subscription OR is within `FREE_TIER_JOB_LIMIT` posts this calendar month (both read from `SystemConfig` via `SystemConfigService`, not hardcoded); otherwise it's rejected at creation with a prompt to subscribe. No more `PENDING_PAYMENT`/`PENDING_SUBSCRIPTION` states. `SCHOOL_PAID_ENABLED`, `FREE_TIER_JOB_LIMIT`, and `TEACHER_PAID_ENABLED` are now real seeded `SystemConfig` settings (they weren't seeded anywhere before — settings existed only conceptually in this doc). | Found mid-implementation: the code and this doc had silently diverged. Confirmed with client to fix the code to match the doc, not the other way around. |
| D44 | **Chat unlock state fixed to be config-aware, matching §2's toggle-driven application flow.** `ChatService.verifyAccess()` was hardcoded to `PAID`/`WON` only. Since `TEACHER_PAID_ENABLED` defaults to `false` (flow: `INTERESTED → SHORTLISTED → WON \| CLOSED`, `PAID` never occurs), chat could **never unlock** in the default/production config — a live bug, not a missing feature. Now: unlocks at `SHORTLISTED` (+`WON`) when `TEACHER_PAID_ENABLED` is off, at `PAID` (+`WON`) when it's on. Applied consistently to both the gateway's access check and the recruiter chat-list endpoint (`ApplicationsService.paidForSchool`, which had the identical hardcoded bug). New `GET /public/settings` endpoint + `usePublicSettings()` FE hook added so the frontend can read the same toggle for its own list-filtering logic without duplicating server config. | Discovered while scoping the "chat after shortlist" feature request — this was blocking it entirely. |
| D45 | **Admin parity brought up to RxJobs4U's level.** Ported: (a) user soft-delete + restore (`deletedAt` on User — already existed but was unused; new `DELETE/PATCH /admin/users/:id[/restore]`, cascades to the owned school/jobs/applications/subscriptions, `includeDeleted` list toggle, two confirm dialogs + dimmed "Deleted" row state in `/admin/users`); (b) a new **Legal Pages** admin editor (`LegalContentModule` — fulfills the already-logged D42 from §9) with `/terms`/`/privacy-policy` now rendering from `SystemConfig`-adjacent `legal_pages` collection instead of static JSX; (c) a new **Email Templates** admin editor (`EmailTemplatesModule`, 14 seeded templates — SOS one dropped per D42-superseding — wired into every transactional email send site with a DB-template-first / hardcoded-string-fallback pattern, matching RxJobs4U's `renderFromDb` approach exactly). | Client asked for "everything RxJobs4U admin has," naming soft-delete/restore as the concrete example; Legal Pages was already a logged-but-undone decision (old D42). |
| D46 | **Security hardening pass — closed the gap between what `CLAUDE.md`'s "Security scaffolding shipped" section claimed and what the code actually did.** An audit found several D15-D24 items were either dead code or missing entirely despite being marked done: Joi env validation (D20) was never wired into `ConfigModule.forRoot()` — fixed. `safeEqual` (D17) existed but the Razorpay webhook still used a timing-unsafe `!==` — fixed. `tokenVersion` (D21) didn't exist anywhere — added to the User schema, JWT payload/strategy, and bumped on password change/reset and admin delete/restore. `verifyUploadKey` (D18) didn't exist — added, wired into `SchoolsService`/`UsersService` on every S3-URL-bearing field. Anonymous auth audit (D24, `logAuthEvent`) didn't exist — added, `AuditLog.adminId` made nullable to support it. `redactEmail`/`redactPhone` (D23) existed but had zero call sites — wired into every raw email/phone log interpolation in `auth.service.ts`/`whatsapp.service.ts`. Additionally found and fixed two gaps not in the original D15-D24 list: `CsrfGuard` existed but was never applied to `/auth/refresh`/`/auth/logout` — fixed (frontend's `apiClient` already sent the required `X-Requested-With` header, so no FE change needed); no `@Throttle` rate-limiting existed on login/OTP/refresh/logout — added, matching RxJobs4U's per-route limits. | Client asked to "implement all security related stuff we did in rxjobs" — the audit revealed the prior claim of "shipped" was partly aspirational, not actual; this closes that gap for real, verified by a clean boot + `tsc --noEmit` on both apps. |

---

## 13. Phase 2 — Recruiter/School area cleanup — 2026-07-09

| # | Decision | Rationale |
|---|---|---|
| D47 | **Fixed a live production bug introduced by D41's rename.** D41 renamed `hospitalInfra`→`campusFacilities` and `hospitalStrength`→`schoolStrength` on the backend `School` schema/DTO, but the frontend (`lib/api/schools.ts`, the School Profile page, `lib/validations/profile.ts`) was never updated to match. With the backend's `forbidNonWhitelisted: true` validation, **every School Profile save was returning HTTP 400.** Fixed by renaming the frontend fields to match; also renamed the three still-hospital-shaped fields the D41 pass missed: `noOfOperationTheatres`→`noOfClassrooms`, `noOfCabinsAndBeds`→`noOfLabsOrSpecialRooms`, `noOfBeds`→`studentCapacity` (front + back, plus every consumer: admin school-detail dialog, teacher-side school-reveal card). | Found while starting Phase 2 — a rename pass that isn't verified end-to-end can silently break the exact form it touched. |
| D48 | **`Subject`/`TeacherPost` enums expanded, new `JobDepartment` enum added — see §3.** The job-posting form (`/recruiter/jobs/new`, `/recruiter/jobs/[id]/edit`) had never adopted the real enums at all: it used local, duplicated, drifted string-array constants (`DEPARTMENT_OPTIONS`/`ROLE_OPTIONS` — edit page had 2 extra role options `new` didn't) plus a second unsynced "subject" vocabulary (`EXPERTISE_OPTIONS`/`DEPARTMENT_REQUIREMENTS_OPTIONS`). Backend `CreateJobDto`/`Job` schema had `department`/`role`/`departmentRequirements`/`specializations` all as loose unvalidated strings. Now: both forms are enum-driven (`Combobox`/`ExpertiseSelector` reading `Object.values(enum)`, a new shared `enumLabel()`/`enumComboboxOptions()` helper in `lib/utils/enum-options.ts` formats SCREAMING_SNAKE_CASE for display), and the backend validates with `@IsEnum`. Client confirmed: expand the enums to match what the live UI already offered rather than trim the UI down to the narrower originally-documented lists. | Same class of bug as D43 (job.department was) — a form drifted from its own backend contract because nothing enforced the enum at either end. |
| D49 | **Teacher Settings "Job Alerts" toggle fixed** (`app/(app)/settings/page.tsx` sent `alertFtJobs`, backend only accepts `alertNewJobs` since D41's SOS-alert consolidation) — another instance of the D47/D43 bug class, found and fixed in passing since it was a 2-line isolated fix, not a scope-creep into the full Teacher-profile rework still deferred to Phase 3. | Cheap, isolated, definitely-broken — fixed on sight rather than deferred. |

**Still deferred to Phase 3** (Teacher profile area — `app/(app)/profile/page.tsx`): `seekerProfileSchema` in `lib/validations/profile.ts` still has `placeOfPractice`/`typeOfPractice`/`medicalCouncilName`/`isRegisteredInCouncil` field names that don't match the backend's already-renamed `currentSchool`/`employmentType`/`boardRegistrationName`/`isRegisteredWithBoard` — **this is the same class of live-breaking bug as D47, just not yet fixed.** Flagging explicitly so Phase 3 doesn't miss it: the Teacher profile save is likely broken the same way the School profile save was.

---

## 15. Phase 3 — Teacher/Job Seeker profile area — 2026-07-09 (COMPLETE)

Picks up the D49 flag exactly as predicted: the Teacher profile save is broken the same way the
School profile save was (D47). Plan approved and executed 2026-07-09, verified live. See
`NewChatStartOff.md` in this folder for the full current-session handoff if picking this up in a
fresh chat.

| # | Decision | Rationale |
|---|---|---|
| D50 | **Phase 3 executed and verified live**: (1) fixed the confirmed 400-bug — 4 stale field names (`placeOfPractice`/`typeOfPractice`/`medicalCouncilName`/`isRegisteredInCouncil`) across 5 frontend files (`app/(app)/profile/page.tsx`, `lib/validations/profile.ts`, `lib/api/users.ts`, `common-components/seeker-profile-view.tsx`, `lib/api/applications.ts`) renamed to match D41's backend rename (`currentSchool`/`employmentType`/`boardRegistrationName`/`isRegisteredWithBoard`); (2) repurposed the `academics` field from the leftover-healthcare `Academics` enum (Assistant/Associate/Professor — meaningless for K-12) to the real `TeacherPost` enum, full-stack, relabeled "Current / Highest Post Held" — the now-dead `Academics` enum was deleted from both `shared/enums` files; (3) rewired `expertise`/`interestedToCover` off two ad-hoc, drifted string arrays (`EXPERTISE_OPTIONS`/`INTERESTED_TO_COVER_OPTIONS`, deleted from `lib/shared/constants.ts`) onto the real `Subject` enum, full-stack (`@IsEnum`/Mongoose `enum:`/Zod `z.nativeEnum`), matching the exact pattern D48 already shipped for job `specializations`. `DEGREE_OPTIONS`/`degrees` and `indemnityInsurance` stayed untouched (no real `Degree` enum exists; `indemnityInsurance` is a dead field pending client input, both already noted in §14). Confirmed via live curl: old field names now 400 (proving the bug was real), new field names 200 and round-trip correctly, invalid `TeacherPost`/`Subject` values now 400 (proving `@IsEnum` is actually enforced, not just typed). No real user data existed in the DB, so no migration was needed for the breaking `academics`/`expertise`/`interestedToCover` value changes. | Continues the same "verify live, don't trust that a rename compiled" discipline established in Phase 2 (D47/D48) — this was the third occurrence of the identical bug class (D43 → D47 → D50). |
| D51 | **Found and fixed a 6th file with the same bug class, missed by the original Phase 3 research** — `applications.service.ts`'s `jobApplicants()` aggregation (`$project` pipeline for the recruiter's "View Applicant" screen) never included `isRegisteredWithBoard`, `boardRegistrationName`, or `desiredCities` in its field projection at all, and had a stale `expectedSalaryLakhs` field that doesn't exist in the schema (the real field is `salaryRange`) — so those fields would always render blank/`—` in the recruiter's applicant view regardless of any frontend fix. Found by live-testing the full apply→shortlist→view-applicant flow with curl, not by code review. Fixed: projection now includes all 4 correct field names. | Direct proof of the session's standing rule that a clean `tsc --noEmit` is not sufficient evidence — this bug was invisible to the type checker (Mongo aggregation `$project` objects aren't type-checked against the schema) and would only surface by actually calling the endpoint. |
| D52 | **`DATA_MODEL.md` and `PROJECT_BLUEPRINT.md` fully rewritten from ground truth** — both predated implementation and had drifted badly: wrong `Role` values (`SCHOOL_ADMIN` instead of `RECRUITER`), a `Posting`/`PostingType`/`PostingStatus`/`GradeLevel`/URGENT-vs-PERMANENT model that was never built (superseded by D42-superseding), `teacherProfile`/`schoolAdminProfile` field names that don't exist (real names: `seekerProfile`/`recruiterProfile`), a two-collection chat model with `body`/`readByRecipient` fields (real: flat `chat_messages` with `text`/`read`). Both docs rewritten directly against the actual `.schema.ts` files and `shared/enums/index.ts`, spot-checked with live greps, not assumed from memory. **Found in passing while doing this**: `RecaptchaService` exists (`common/recaptcha/`) but has zero call sites outside its own file — it is not wired into any controller/guard despite `PROJECT_BLUEPRINT.md`'s old claim that it gates register/login/OTP-send/forgot-password. Also, `POST /auth/register` has no `@Throttle` decorator (login/OTP-send/OTP-verify/refresh/logout all do). Neither is fixed yet — flagged for the upcoming end-to-end testing / close-off pass, not silently left undocumented. | User: "we don't want any stale" — direct instruction after DATA_MODEL.md's staleness was raised. The reCAPTCHA/register-throttle gaps are the same "shipped in docs, dead/missing in code" pattern D46 already found once; worth a deliberate look before calling auth "done," not urgent enough to interrupt the docs pass to fix immediately. |
| D53 | **Independent audit of all Phase 1 (D41-D46) and Phase 2 (D47-D49) claims, re-checked against current code by a fresh pass — all 9 CONFIRMED, zero BROKEN.** Phase 3 (D50-D51) was already live-verified via curl the same day it shipped, so this audit covered the two older phases instead. Every rename, every security-hardening item (Joi validation actually wired, `safeEqual` actually used, `tokenVersion` actually checked, `verifyUploadKey` actually called, `CsrfGuard` actually applied, throttle limits matching exactly), the admin cascade-delete logic, and the enum consolidation all check out against the real code, not just the doc's description of it. One cosmetic-only finding: `noOfCasesPerMonth` (a leftover healthcare-domain identifier from `job.schema.ts` down through both job-form pages) was never renamed at the field-name level — UI label is correctly "No. of Students per Class," so it's not a functional bug, just an internal-naming leftover, low priority. | User: "we can see without any hallucinations phase1,2,3 are perfect" — explicit request for a rigor pass before moving to end-to-end testing and closing off the project. This confirms the bar is met; Phase 1-3 work is solid, not just claimed solid. |
| D54 | **reCAPTCHA v3 fully wired end-to-end, closing the D52 gap** — mirrored RxJobs4U's exact pattern. Backend: `RecaptchaService` added to `AuthModule` (needed `SystemConfigModule` import for `getSecret()`), `RecaptchaService.verify(token, action)` called in `AuthController.register`/`sendOtp`/`forgotPassword` (before delegating to the service) and in `AuthService.login()` (after credential validation, so auth errors surface first — matches RxJobs4U's own ordering comment). `recaptchaToken?: string` added to `RegisterDto`/`LoginDto`/`SendOtpDto`; `forgotPassword` takes it as a second `@Body()` param since it never had a DTO. Frontend: `RecaptchaProvider` (already existed, defined but never mounted) is now wrapped around the app in `common-components/providers.tsx`; `useGoogleReCaptcha().executeRecaptcha(action)` called before submit on all 4 auth pages (register, login, otp-verify — all 3 send-OTP call sites: auto-send, manual send, resend — and forgot-password), token passed through `lib/api/auth.ts`. Also added `@Throttle({limit:3, ttl:60_000})` to `register` (previously unthrottled — the exact D52 gap) and to `forgot-password` (found unthrottled too while doing this, same fix). Live-verified: register→OTP-verify→login round-trip still works (dev-mode `RecaptchaService` skips verification by design — `NODE_ENV !== 'production'` — so this only proves the wiring doesn't break the happy path, not that a real score-check works; that needs a prod-config test later), and the new register throttle actually returns 429 on a 4th rapid attempt within 60s. Google Maps (`NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`, loaded via `<Script>` in `app/layout.tsx`) and Google OAuth (`@react-oauth/google`, `GoogleOAuthProvider` + `useGoogleLogin`, backend access-token→userinfo flow) were both **already fully wired** — checked, not touched. | User provided fresh working keys for gmaps/recaptcha/google-oauth in both `.env` files and asked to "wire them up properly like we did in RxJobs" — direct continuation of the D52 finding. |
| D55 | **CRITICAL bug found and fixed during E2E close-off testing: the default hiring flow (`TEACHER_PAID_ENABLED` off) could never reach `WON` at all, and every shortlisted application would have been silently auto-closed after 48 hours regardless of config.** Three compounding bugs in `applications.service.ts`, all instances of the exact bug class D44 already fixed once for chat gating (hardcoded to PAID/WON instead of reading the config toggle): (1) `markWon()`'s `allowedStates` was hardcoded to `[PAID]` only — under the default config, applications never reach `PAID` (there's no payment step), so **no recruiter could ever mark anyone WON, at all, ever, in the default configuration** — this is the core hiring action of the entire platform. (2) `shortlist()` unconditionally set `paymentDueBy` to a 48h deadline regardless of the toggle. (3) `runPayWindowSweep()` (a cron job running every 5 minutes) unconditionally auto-CLOSED any `SHORTLISTED` application past its `paymentDueBy` — combined with (2), this meant **every single shortlisted application on the platform would auto-close itself within 48 hours**, config or no config, since MongoDB's BSON type ordering means a `null` date field still matches a bare `{$lte: someDate}` query (null sorts below Date). Fixed all three: `markWon` now computes `allowedStates` from the toggle (`[SHORTLISTED]` when off, `[PAID]` when on); `shortlist()` only sets `paymentDueBy` when the toggle is on (`null` otherwise); `runPayWindowSweep()` short-circuits to a no-op when the toggle is off, plus an explicit `paymentDueBy: {$ne: null}` guard as defense-in-depth against the BSON-null gotcha. Also fixed the shortlist notification body, which unconditionally told every teacher "Pay ₹99 within 48 hours..." even when no payment was ever required. Verified live: full `INTERESTED → SHORTLISTED → WON` cycle now works end-to-end under the default config, job correctly flips to `FILLED` with `filledPositions` incremented. **Separately noted, not fixed (low severity, pre-existing, scope decision not a bug):** `EmailService.sendShortlistNotification()` exists, has a seeded email template (`SHORTLIST_NOTIFICATION`), but is never called from anywhere — shortlist notifications are in-app/push only, no email ever goes out on shortlist. Not fixed this pass since it's a missing-feature gap, not a wrong-behavior bug; flagged for a future decision on whether email-on-shortlist should exist. | Found by literally running the recruiter's "mark WON" action during the agreed manual E2E walkthrough — would never have surfaced from `tsc --noEmit`, code review, or even the D53 audit (which didn't touch this code path). This is the single most severe bug found this entire project — it broke the platform's core value proposition (schools hiring teachers) under the documented default configuration. Directly justifies the user's insistence on live E2E testing before considering Phase 1-3 "closed." |
| D55a | **Second silent config-key mismatch found during the same E2E pass**: the seeded, admin-editable setting was named `POSTING_ALERT_RADIUS_KM` (`system-config.service.ts` seed) but `NotificationsService`'s job-alert fan-out actually reads `JOB_ALERT_RADIUS_KM` — a different key entirely, a leftover from the pre-D41 "Posting" terminology that never got renamed when the rest of the domain moved to "Job." Because `getSettingNumber()` silently falls back to its hardcoded default (30) on a missing key rather than erroring, this setting was **completely inert** — an admin changing "Posting Alert Radius" in Settings had zero effect on the actual notification radius, with no error or warning anywhere. Fixed by renaming the seed key/label/description to `JOB_ALERT_RADIUS_KM`/"Job Alert Radius" to match the actual consumer. Verified live: new correctly-named row auto-seeds on restart, admin `PATCH` against it now returns 200 and actually reaches the value the notification fan-out reads. The old `POSTING_ALERT_RADIUS_KM` row is left as harmless orphaned dev data (no delete-setting endpoint exists by design) — will disappear on the next DB reset already documented in `DATA_MODEL.md` §5. | Same root cause pattern as D55 (a silent mismatch between what admin configures and what code reads) but lower severity — it degrades one alert radius to a fixed default rather than breaking the hiring loop. Caught only by cross-referencing the settings-list API response against the actual `getSettingNumber()` call site during the admin-journey E2E pass, not by any static check. |
| D60 | **Root cause of the "chat completely silent" report found: a real env-var/namespace bug, affecting both Chat and real-time Notifications.** After D59's fixes didn't resolve it, added diagnostic logging (`console.log`/`connect_error` handling) and re-derived the actual failure: both chat pages (and, independently discovered while investigating, `notifications-bell.tsx`) construct their Socket.IO connection URL from `process.env.NEXT_PUBLIC_API_BASE_URL`, expecting a **bare origin** (`http://localhost:3001`, no path) — the backend's `app.setGlobalPrefix('api')` only applies to REST controllers, not WebSocket gateway namespaces, so a socket must connect to `${origin}/chat` or `${origin}/notifications`, never `${origin}/api/chat`. But `.env.local` had `NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api` — the REST-style value — because **the same env var name is used elsewhere in the codebase (`lib/api/public.ts`, `app/terms/page.tsx`, `app/privacy-policy/page.tsx`) expecting the opposite shape** (`/api`-suffixed, for REST calls). No single value could satisfy both sets of consumers; whichever shape you pick, the other set silently breaks. With the REST-style value in place, every chat socket was connecting to a namespace that doesn't exist (`/api/chat` vs the real `/chat`) — Socket.IO's default behavior in that case is a `connect_error`, which nothing in the frontend listened for before this session (D59 added the listener, but the underlying URL was still wrong, so it would have started surfacing the real error on retest — never got the chance before this fix). Fixed properly: added `SOCKET_ORIGIN` to `lib/api-client.ts`, derived from the same `NEXT_PUBLIC_API_URL` the REST `apiClient` already uses (stripping a trailing `/api`) — a single source of truth instead of a second, ambiguously-shaped env var. Both chat pages and `notifications-bell.tsx` now import `SOCKET_ORIGIN` instead of reading `NEXT_PUBLIC_API_BASE_URL` directly. `public.ts`/`terms`/`privacy-policy` were left untouched (their usage of `NEXT_PUBLIC_API_BASE_URL` for REST calls is a separate, pre-existing pattern, out of scope for this fix — worth a follow-up pass to also consolidate onto `NEXT_PUBLIC_API_URL` for consistency, but not blocking). | User's manual browser test caught this — a bug invisible to every curl-based verification this entire project, since curl never goes through the frontend's env-var resolution or Socket.IO's namespace matching at all. Direct proof that D56's "no browser testing" gap was real: this bug could not have been found any other way. **User confirmed both chat and the notification bell working live after the fix + frontend restart.** |
| D59 | **First real browser-driven bugs found — chat page, reported directly by the user testing manually.** Four issues: (1) **`GET /users/me` returned a raw Mongoose document (`_id` only), not the `SafeUser` shape (`id`) that login/OTP already return** — `users.service.ts`'s `getMe()` never used the existing `toSafeUser()` mapper (was private to `auth.service.ts`). Any frontend code doing `user.id` after a page reload (not fresh login) silently got `undefined` for the rest of that session. This is exactly why Teacher-side chat message alignment collapsed to one side after reload: `isMine = msg.senderId === user?.id` was `false` for every message, permanently, once `user.id` was undefined — not a timing race, a real structural gap. Fixed by extracting `SafeUser`/`toSafeUser` to a shared `src/shared/utils/safe-user.ts` and having both `auth.service.ts` and `users.service.ts` use the same mapper — `getMe()` now returns the correct shape. (2) **Chat container height was wrong** — both chat pages used `h-[calc(100vh-4rem)]` accounting only for the `h-16` header, not the shared layout's `<main className="p-6 ...">` wrapper (3rem more vertical padding), causing page-level overflow scroll instead of a clean fixed-height panel. Fixed to `h-[calc(100vh-7rem)]` on both. (3) **No `chat:error` handling anywhere on the frontend** — the backend gateway does emit `chat:error` on a failed join/send (bad auth, access denied), but neither chat page ever listened for it, so any failure was completely silent: input cleared, no message appeared, zero feedback. Added a `chat:error` listener + toast on both pages. (4) **No auto-select of the first conversation** — user explicitly asked for this; added on both pages. Also hardened the join/send race: `chat:join` was emitted unconditionally even if the socket hadn't finished connecting yet; now guarded by `socket.connected` plus a `connect`-event re-join using a ref, so an auto-selected conversation (which can now fire before the socket handshake completes) still joins correctly. Live-verified the core join→send→broadcast pipeline directly via a Node `socket.io-client` script mirroring the exact browser flow — confirmed working end-to-end before and after these fixes, meaning the underlying gateway/service logic was never broken; all four bugs were frontend-only. | User ran the manual E2E checklist (`E2E_TEST_CHECKLIST.md`) themselves for the first time and hit real bugs within the first feature tested (chat) — direct proof that the "browser/UI never tested" gap flagged in D56 was real and worth closing, not just a formality. |
| D58 | **Job listing duration made admin-configurable** — was a hardcoded `JOB_TTL_MS = 30 days` constant in `shared/constants/pricing.ts`, used at 3 call sites (job creation, and two job-reactivation/"Boost" paths in `jobs.service.ts` and `payments.service.ts`). Moved to a new `SystemConfig` setting `JOB_LISTING_DURATION_DAYS` (default 30, range 7-90 days, matching the `FREE_TIER_JOB_LIMIT`/`TEACHER_PAID_ENABLED`/`JOB_ALERT_RADIUS_KM` pattern), read via a new `SystemConfigService.getJobListingDurationMs()` helper so the 3 call sites can't drift on the day→ms conversion. 30 days was kept as the default since it's already in the industry-normal 30-45 day range (LinkedIn/Indeed/Naukri) — the ask was to make it editable without a deploy, not to change the number. **Found and fixed a second, unrelated bug while verifying this live**: the admin settings-update endpoint (`PATCH /admin/config/settings/:key`) reads `@Body('value')`, but silently accepted (200, no error) a request sending `valueNumber` instead — Mongoose drops `undefined` from a `$set`, so the setting just never changed with zero feedback to the admin. Added an explicit `typeof valueNumber !== 'number'` guard that now correctly 400s. Verified live: setting an incorrect field name now errors; setting `JOB_LISTING_DURATION_DAYS` to 45 and posting a new job produced `expiresAt` 45 days out (not 30); reset back to 30 after the test. | User pushed back on the hardcoded 30-day figure and asked for "standard industry approach" with room for discussion — recommended keeping 30 days (already standard) but making it a no-deploy admin setting; user agreed. The `value`/`valueNumber` field-name bug was found purely by accident during live verification of this change, not sought out — another instance of the session's running theme that untested endpoints silently do nothing instead of erroring. |
| D59 | **Email delivery migrated from Brevo to Gmail SMTP/OAuth2 via nodemailer, matching RxJobs4U exactly.** `EmailService` was calling Brevo's transactional HTTP API (`api.brevo.com/v3/smtp/email`, `BREVO_API_KEY`) despite `env.validation.ts`, `.env.example`, and `SECRETS_ROTATION_RUNBOOK.md` all already describing a Gmail SMTP setup as if it were live — a migration that was apparently done in config/docs but never actually done in code. Rewrote `EmailService`'s constructor (now builds an `nodemailer.createTransport({ service: 'gmail', auth: { type: 'OAuth2', ... } })` reading `GMAIL_USER`/`GMAIL_CLIENT_ID`/`GMAIL_CLIENT_SECRET`/`GMAIL_REFRESH_TOKEN` directly via `ConfigService`, no DB-stored secret) and `send()` (now `transporter.sendMail()`, skip-with-warning if `GMAIL_REFRESH_TOKEN` is unset) to mirror RxJobs4U's implementation line-for-line. Removed the now-unused `SystemConfigService` dependency from this file (Brevo was its only consumer). Fixed two branding leftovers found in the same files: `SMTP_SENDER_NAME` defaulted to `'RxJobs4U Admin'` in both `env.validation.ts` and `.env.example` — now `'SchoolTeacher'`. **Verified live, not just compiled**: backend restarted clean with real Gmail OAuth2 credentials (temporarily borrowed from RxJobs4U's own `.env` — client-authorized, for pipeline-verification purposes only, explicitly flagged for removal before production once EduHire has its own dedicated Gmail account — see the pending item in `NewChatStartOff.md`), registered a fresh test account with the project owner's real email, and confirmed `EmailService` logged `Email sent → poornavikas99@gmail.com` — a real Gmail send, not a dev-mode skip. | User: "we need to change it to Gmail SMTP... same like in rxjobs" — explicit instruction to actually migrate the code, not just flag the mismatch. Confirmed AWS S3 already verified working separately; this closes the last outstanding third-party integration gap before Phase 4 (chat) is the only thing left pending. |
| D60 | **`E2E_TEST_CHECKLIST.md` written** — a full manual pre-production QA script covering every real route in the app (38 `page.tsx` files inventoried, not guessed), organized by role: public pages, shared auth, Teacher, Recruiter/School, Admin, plus cross-cutting checks (mobile, session expiry, role-gating, direct-URL access). Items known-fragile from earlier live-verified bugs (D47/D50/D55/D57/D58/D59) are flagged so they get extra attention. Also published as an interactive checkbox artifact with localStorage-persisted progress, per-section counts, and a "critical" flag on the same fragile items. This is intentionally the FIRST pass this whole project that will exercise a real browser — everything through D59 was API-level (curl) verification only, which cannot catch a visually broken page. | User: "gve me full E2E tetsing fow of eveyr role, screens, button clcik. i will check and mke this into production" — they're taking over manual QA from here; this is the handoff artifact for that. |
| D57 | **Admin job delete added** — admin could previously only disable a job (`DISABLED_BY_ADMIN` status, reversible-ish, job stays in DB) but had no way to remove one outright, unlike recruiters who already had `DELETE /jobs/:id` on their own jobs. Added `DELETE /jobs/admin/:id` (`adminRemoveJob()` in `jobs.service.ts`), mirroring the recruiter's existing `remove()` cascade exactly: soft-delete (`deletedAt`), close all open applications on that job with a decision reason, notify affected teachers — plus an audit log entry (`JOB_DELETED`), matching the pattern already used for `adminDisableJob`. Soft delete chosen deliberately, matching the codebase-wide convention (`deletedAt` everywhere) rather than a hard delete, which would orphan chat messages/applications/payments still referencing the job and lose the audit trail. Frontend: `Delete` button added next to `Disable` on the admin Jobs page, its own confirm dialog. Verified live: job removed from admin list, audit log entry present with correct before/after. | User asked whether job deletion existed for recruiters/admin and which approach (hard vs soft) was better; chose soft delete + admin parity via `AskUserQuestion`. |
| D61 | **Admin panel retrofitted with RxJobs4U's tabs-based data segregation pattern, across every list page that has a real RxJobs4U counterpart with tabs.** A research pass (agent-driven, comparing all 7 remaining EduHire admin pages against their RxJobs4U equivalents) found only 3 of 9 total admin pages actually needed the change: `app/admin/schools/page.tsx` (pill/segmented-control tabs ALL/PENDING/VERIFIED/REJECTED, wired into the TanStack Query `queryKey`, replacing a "Verification Status" dropdown — mirrors RxJobs4U's `hospitals` page), `app/admin/config/page.tsx` (shadcn `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` wrapping the 4 previously-stacked sections — Platform Settings/API Keys/Email Templates/Legal Pages — mirrors RxJobs4U's `config` page, minus its "Cron Jobs" tab since EduHire has no cron feature), and `app/admin/users/page.tsx` (pill tabs All Users/Teachers/Schools on `role`, replacing the Role dropdown in the Filters modal — mirrors RxJobs4U's `users` page). The other 4 candidate pages (`jobs`, `payments`, `disputes`, `audit`, `pricing`, `analytics`) were confirmed to need **no change**: RxJobs4U's `jobs`/`payments` tabs exist only because of its SOS-vs-Full-time job-type split, which D42-superseding already removed from EduHire's domain entirely — retrofitting a tab for a dimension that doesn't exist would mean fabricating a filter with only one real value. `disputes`/`audit`/`pricing`/`analytics` have no tabs pattern in RxJobs4U either, so EduHire's existing dropdown/no-filter approach on those pages already matches. All three retrofitted pages typecheck clean (`tsc --noEmit`). **Process note, not a decision**: an accidental `npx prettier --write` on `config/page.tsx` (wrong style for this project — double quotes/aggressive wrap vs. established single-quote/long-line convention) followed by a `git checkout --` intended to undo just that formatting pass instead reverted the entire uncommitted Tabs retrofit on that file, since `git checkout --` has no concept of "just the last change." Redone from scratch by hand, this time without a blanket formatter pass. | User: "for all pages we have tabs and segregated data based don it in rxjobs admin. use same tabs component here, we need proper segregation same as in rxjobs admin" — asked via `AskUserQuestion` which pages needed it, user selected "All list pages." The research pass established that "all pages" in practice meant "all pages where RxJobs4U's own tabs reflect a real EduHire data dimension," not a blanket mechanical copy onto pages where the underlying distinction (SOS vs Full-time) had already been deliberately removed. |
| D62 | **Role-layout render-guards and logout navigation fixed to match RxJobs4U's already-shipped pattern** (`BUG_PATTERNS.md` FE-15/FE-16/FE-17) — EduHire's copies of these three files never actually picked up the fix, despite the pattern being documented. Confirmed by diffing all three layouts (`app/admin/layout.tsx`, `app/recruiter/layout.tsx`, `app/(app)/layout.tsx`) plus `common-components/app-header.tsx`'s logout handler line-for-line against RxJobs4U's current versions. **(1) FE-15 (flash/stale render of wrong-role content):** all three layouts redirected the wrong role away via a `useEffect`, but the render path only gated on `isLoading` — so a role mismatch rendered that layout's children (the actual dashboard/table/page content) for a render cycle before the async redirect fired, and if `user` state changed later without a remount (see next paragraph), it stayed rendered indefinitely instead of just flashing. Fixed: every layout's guard is now `if (isLoading || !user || user.role !== <ThisLayoutsRole>) return <Spinner/>`, and each `useEffect` now redirects every other role to its own correct home (previously admin/recruiter/teacher layouts only handled 1-2 of the 2 other roles each, falling back to a generic `/dashboard` bounce that then had to redirect a second time). **(2) FE-16/FE-17 (logout leaves Back navigable into the app, and leaves stale cross-role cache):** `app-header.tsx`'s `handleLogout` used `router.push('/login')` with no `queryClient.clear()` — a Next.js client-side push that keeps the protected page in history (Back after logout could re-enter, or briefly flash old TanStack Query cache from the just-ended session) — RxJobs4U's already uses `queryClient.clear(); window.location.replace('/login')` (a hard navigation that overwrites the history entry and forces every provider, including `AuthContext`, to remount from scratch). Fixed to match; `router`/`useRouter` became unused in that file as a result and were removed. **Root cause of the specific symptom reported** ("logged in as Admin in one tab, came back an hour later, saw the School dashboard, had to click through routes to get back to Admin"): the refresh-token cookie is shared across same-origin tabs; testing multiple roles in the same browser lets a later login in one tab silently swap the cookie for every other tab. `apiClient`'s 401-refresh interceptor (`lib/api-client.ts`) then silently obtains a new access token for the *other* account on 401 without re-fetching `getMe()`/updating `AuthContext.user`, so the tab's UI state and its actual authenticated identity diverge until something re-triggers `getMe()` (e.g. a layout mount) — at which point `user.role` flips out from under the render. The cookie-sharing itself is inherent to cookie-based auth for multi-role testing in one browser (RxJobs4U has the identical limitation; use separate browsers/profiles/incognito windows per role for concurrent testing) and is not something a per-tab fix can fully eliminate — but the render-guard fix means that once the mismatch surfaces, the layout now immediately spinners-then-redirects to the correct role home instead of rendering the wrong dashboard's content and requiring manual navigation to escape. Verified via `tsc --noEmit` clean on both apps; not yet re-verified live in-browser by the user as of this entry. | User: "we need same level of auth checks here aswell like in rxjobs... afer hour opened it, now its diff dashboard... also ensure clicking back wont take us to login after loginnin, like navigation fix we did in rxjobs all thos" — directly reporting the FE-15/16/17 symptoms in EduHire despite those patterns already being documented in `BUG_PATTERNS.md` from the sibling project; the docs correctly described the fix, but this specific copy of the code had never applied it. |
| D63 | **Mobile-responsiveness pass on the landing page, all 4 auth pages, and both legal pages — one confirmed overlap bug fixed, plus a broad "everything is oversized on mobile" cleanup.** User reported real overlapping elements ("some buttons and all") and generally oversized content on mobile. Code audit (no visual browser tool was available this session — see caveat below) found: **(1) a genuine overlap bug** — the landing page's Bento-grid "Every school admin-verified" cell had a "Verified School ✓" badge `absolute`-positioned at `bottom-right` inside a `minHeight: 200` cell; on a single-column mobile layout the body paragraph above it can wrap to 3 lines and grow past 200px, so the absolutely-positioned badge would sit on top of the paragraph's last line. Fixed: badge is now in normal document flow (`mt-5`) below the paragraph on mobile, `sm:absolute sm:bottom-6 sm:right-6` only once the layout has the two-column width where the fixed minHeight actually guarantees clearance. **(2) The OTP segmented input** (`otp-code-input.tsx`, used on both `/otp-verify` and `/forgot-password`) rendered 6 fixed `w-11` (44px) boxes with `gap-2 justify-between` and no `min-width:0` — on screens under ~360px wide the combined intrinsic minimum width of 6 `<input>` elements can exceed the available card width, which (combined with the inputs' default non-zero `min-width`) risks the row overflowing the card rather than shrinking cleanly. Fixed: boxes are now responsive (`w-8 h-10` mobile → `sm:w-11 sm:h-12`), gap reduced on mobile, `min-w-0` added. **(3) `RoleToggle`** (register page's "I'm a Teacher"/"I'm a School" pills, and the Google-role selector on login) sits inside a `flex-1` pair, but the underlying `Button` component's base class includes `whitespace-nowrap` and `shrink-0` — on the narrowest phones this risks text clipping/overflow rather than wrapping. Fixed: added `min-w-0` and made the label `text-xs sm:text-sm`. **(4) The broad oversizing issue**: most landing-page section paddings (`py-20`/`py-24`/`py-28`), several headline `clamp()` font sizes, and a few card paddings (`p-8`/`p-10`) were fixed values applied uniformly at every breakpoint — worse, several `clamp()` calls (hero H1, stats numbers) had a `vw`-based middle term too small to ever exceed the `rem` floor below ~800px viewport width, meaning the "responsive" font size was actually a flat desktop-scale constant on every phone. Fixed throughout `app/page.tsx`: every section padding, card padding, and heading/number font size now has an explicit mobile value with `sm:`/`md:` step-ups, and every previously-pinned `clamp()` floor was lowered so it actually shrinks below ~420-800px (e.g. hero H1 `clamp(2.8rem,5.5vw,4.4rem)` → `clamp(2.1rem,8vw,4.4rem)`; stats numbers `clamp(3.5rem,7vw,5.5rem)` → `clamp(2.25rem,9vw,5.5rem)`). Also applied lighter versions of the same padding/font fixes to the `(auth)/layout.tsx` card (`p-8`→`p-5 sm:p-8`) and both legal pages (`terms`/`privacy-policy`: `py-16`→`py-10 sm:py-16`, heading/icon sizes stepped down on mobile). **Caveat, stated plainly**: this session had no browser/screenshot tool available, so the fixes are a thorough code-level audit (traced actual box-model math against 320-375px viewport widths) rather than a live-verified visual pass — the project's own "verify live, don't trust a clean compile" standard (D47/D50/D55 etc.) could not be fully honored here. `tsc --noEmit` is clean on the frontend, but the user needs to visually confirm on an actual device or the dev server before this is considered closed. | User: "i need perfect mobile responsiveness for landing page, all auth pages, all legal pages... current landing doesnt look good in all mobile lot of issues" then, mid-session, "few things are being overlapped, like some buttons and all" and "few things appear big for mobile like also see on that perspective" — three separate, increasingly specific complaints in the same request. |
| D64 | **Auth card shrunk further on mobile, and a cookie-consent notice added (previously missing entirely).** (1) Following up on D63: the `(auth)/layout.tsx` card was still full-width-minus-padding on mobile even after D63's padding trim; capped it at `max-w-xs` (320px) on mobile (`sm:max-w-md` unchanged above 640px) and reduced card padding to `p-4` (from `p-5`), plus tightened internal vertical rhythm (headings, form `space-y`, dividers) on all 4 auth pages so the now-narrower card doesn't look disproportionately tall. Re-verified the OTP box math from D63 still fits inside the smaller card at 320-375px — it does, with margin to spare. (2) Confirmed via grep that **no cookie-consent notice existed anywhere** despite the site loading Google Maps unconditionally on every page (`app/layout.tsx`'s `<Script>`) and Google reCAPTCHA/OAuth on the auth pages — all of which set third-party cookies before any user action. Added a lightweight, non-blocking notice (`common-components/cookie-consent.tsx`, mounted in the root layout): a dismissible bottom banner, shown once per browser (localStorage flag, not a cookie itself), linking to `/privacy-policy`. **Deliberately a notice, not a full consent-gate/CMP** — it does not block or delay loading of Maps/reCAPTCHA/OAuth scripts (same "implied consent via continued use" pattern most sites use), since gating those would need a bigger rework (Places Autocomplete is used immediately on the registration/location fields) and wasn't asked for. Also found the linked Privacy Policy had **zero mention of cookies at all** despite the new banner promising it — added a new "6. Cookies & Similar Technologies" section to the `legal-content.service.ts` seed (auto-applies on next backend restart per the same idempotent-seed mechanism used for the D61-era support-email fix, since neither legal page has been admin-edited), renumbering the sections after it (old 6-10 → 7-11). | User: "The Forms Cards in Auth pages can be a bit smaller in mobile screens pls. Also arent we asking for cookies consent permisions lie that?? its better to hav right?" — direct request plus a good-instinct catch that the site had no cookie notice despite loading Google's scripts. |
| D65 | **reCAPTCHA badge scoped to auth pages only (was app-wide), and hidden in favor of the required text disclaimer to avoid fighting the new cookie-consent banner.** User asked whether the floating "protected by reCAPTCHA" badge would be visible on every page — checking `common-components/providers.tsx` confirmed yes: `RecaptchaProvider` wrapped the entire app (root `Providers`, mounted in root `layout.tsx`), so Google's script loaded and its badge rendered on every route — landing, dashboards, chat, admin — even though `useGoogleReCaptcha()`/`executeRecaptcha()` is only ever called from the 4 `(auth)` pages (confirmed via grep, zero other call sites). Moved `RecaptchaProvider` out of the root `Providers` and into `app/(auth)/layout.tsx` so the script only loads where it's used. Separately, while scoping this, realized the badge (fixed bottom-right, Google sets its z-index to 2000000000) would sit directly on top of the D64 cookie-consent banner (also bottom-fixed) on those same 4 pages for a first-time visitor — an immediate, self-inflicted overlap regression from the previous turn's work. Fixed by hiding the badge entirely (`.grecaptcha-badge { visibility: hidden }` in `globals.css`) and adding the Google-required alternative: a small `<RecaptchaNotice />` text ("This site is protected by reCAPTCHA and the Google Privacy Policy and Terms of Service apply", both linked) placed directly under each of the 4 actual recaptcha-triggering submit buttons (login, register, otp-verify's send-OTP step, forgot-password's send-code step — not the second step of either flow, which don't call `executeRecaptcha`). Hiding the badge this way is explicitly permitted by Google's terms as long as the attribution text is shown near the protected action, which this satisfies. | User: "so th recapctha symbol will be vibile contniously throught the app?" — a direct question that surfaced a real app-wide-badge issue (not just a hypothetical), which in turn surfaced the badge/cookie-banner overlap risk that the previous D64 change had just introduced. |
| D56 | **Manual E2E close-off walkthrough complete, all 3 roles.** Full live curl-driven journeys: **Teacher** — register→OTP-verify→login, profile save (Phase 3 fields), apply to a job, chat both directions, hired (WON). **Recruiter/School** — register, school profile, post job, shortlist an applicant, chat, mark WON (job correctly flips to FILLED with `filledPositions` incremented), free-tier job-limit enforcement (2/month, 3rd post correctly rejected), subscription order creation (correctly fails fast on unset pricing per D29's deliberate no-fallback design). **Admin** — stats dashboard, settings list + update (including the newly-fixed `JOB_ALERT_RADIUS_KM`), legal pages (admin + public read), email templates list, user soft-delete → login blocked → restore → login works again, audit log correctly capturing before/after state for both admin actions and anonymous auth events. Two real, previously-undiscovered bugs found and fixed along the way (D55, D55a) — both invisible to `tsc --noEmit`, code review, and the earlier D53 audit, only surfaced by actually running the flows. No further bugs found in the remaining surface tested. **Not covered this pass** (noted, not blocking): full Razorpay checkout completion (webhook signature can't be faked without real test-mode credentials — order creation confirmed working, actual payment completion needs a browser-driven test with real Razorpay test keys), resume/video S3 upload completion (presign flow already verified in Phase 1, not re-run here), WhatsApp OTP verification, admin dispute-ticket flow, and any UI/visual testing (this pass was API-level only — no browser was driven). | User: "we can proceed to next phase and close off the project, like go for end to end detailed testing" — agreed scope was a manual live walkthrough (not automated tests), which is what this covers. The two bugs found justify that choice: neither would have been caught by writing unit tests against the code as it existed, since the tests would have encoded the same wrong assumption the code made. |

---

## 14. Explicit Non-decisions (open, client to confirm)

- Subscription auto-renewal: Razorpay Subscriptions API vs manual re-purchase.
- Chat 500-char limit vs the current 2000-char implementation (see D40).
- Chat file upload (D39) — not yet implemented.
- Verified badges — who / how.
- Teacher / school ratings.
- Referral programme.
- Actual pricing values (D29 sets the mechanism; values TBD).
- `indemnityInsurance` field on the teacher profile — left untouched during the D41 rename pending a client call on whether it's kept, renamed, or dropped.

---

## 11. Carried forward from RxJobs4U — 2026-07-06/07 (not yet built here — bake in when Auth/Admin modules land)

EduHire's `EduHire-Backend` was copied before RxJobs4U shipped these two features (no
`email-templates` or `legal-content` module exists here yet, and there is no `changePassword`
endpoint yet — the whole Auth module is still on the Phase 1 to-do list, see `CLAUDE.md`).
Logging both here now so they get built in from day 1 instead of being rediscovered as gaps
later, the same way `BUG_PATTERNS.md` already inherits RxJobs4U's hard-won patterns.

| # | Decision | Why |
|---|---|---|
| D41 | **`Users.passwordHash` must support `null`, and the Settings "password" panel must distinguish two modes**: users who signed up via Google and have no password yet get a **"Set Password"** form (new password + confirm only, no current-password field); everyone else gets **"Change Password"** (current + new + confirm). Expose a `hasPassword: boolean` on `GET /users/me` and in the login/Google/OTP `SafeUser` response so the FE can branch without guessing. | RxJobs4U shipped Google OAuth signup without ever giving those users a way to set a password short of the Forgot-Password/OTP detour — `changePassword()` hard-threw `BadRequestException('Password login not set up for this account')` whenever `passwordHash` was null. Fixed 2026-07-06 in the sibling project; EduHire's Auth module (Phase 1) should have this from the first cut, not as a v2 patch. See RxJobs4U `BACKEND_GUIDE.md`/`CLAUDE.md` "Admin-editable Legal Pages + business email fix" entry and `users.service.ts changePassword()` for the reference implementation (skip the `bcrypt.compare` check entirely when `passwordHash` is null; still `$inc: tokenVersion` on set). |
| D42 | **Terms & Privacy Policy content should be admin-editable**, not hardcoded JSX — a `LegalContentModule` (`LegalPage` schema: `key: 'TERMS'\|'PRIVACY'`, `title`, `lastUpdatedLabel`, `sections: {heading, body}[]`, `updatedByAdminId`), admin CRUD under the existing `AdminController` (`@Roles(ADMIN)`), and one `@Public()` read route (`GET /public/legal/:key`) for the FE pages to fetch from. Structured sections (not one raw-HTML blob) so admin can add/remove/reorder without touching markup. | Today `/terms` and `/privacy-policy` here are still the static pages copied wholesale from RxJobs4U (see D36 — they were even missing from the `PUBLIC_PATHS` allow-list for a week). RxJobs4U converted these to the pattern above on 2026-07-06, mirroring its existing `EmailTemplatesModule` (admin-editable email copy, shipped Phase 3f). EduHire has neither `email-templates` nor `legal-content` yet — when the Admin module gets built out past the current approval-queue scaffold, both content-editing patterns should land together, reusing the same admin Settings page shape (tabs + card list + edit dialog with a `Textarea`-based section editor, no drag-and-drop lib needed). |

**When picking this up:** update `CLAUDE.md`'s Phase 1 checklist (Auth module, Admin module bullets) to reference D41/D42, and once built, log the actual EduHire-specific implementation notes in `BUG_PATTERNS.md` under a new dated section (matching RxJobs4U's `## Phase 3f patterns` style) rather than editing this entry after the fact.

